{
  "name": "rallion-portfolio-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Optional local Express server serving the same content as frontend/api. Handy for local development without the Vercel CLI. Not used in the Vercel deployment itself.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
