# Leave unset to use the colocated /api serverless functions (recommended
# for `vercel dev` and for the production Vercel deployment).
# Set this only if you want to point the frontend at the standalone
# Express server in ../backend during local development instead:
# VITE_API_URL=http://localhost:4000/api
